`NDOEJS` version: `20.11.0`

`NPM` version: `10.2.4`

Install `node_modules`
``` npm install --save ```

run `api.messager`
``` npm start ```

### Get All Users
`GET /users`

#### Response
```
[
  {
    "id": 1,
    "name": "John Doe"
  }
]
```